#include <cstdio>#include <memory.h>using namespace std;bool  f[510][510],vist[510];int p[510],N,K;bool  dfs(int k){    for ( int i=0; i<N; i++)    {        if (!vist[i]&&f[k][i])        {            vist[i]=1;            int t=p[i];            if (t==-1||dfs(t))            {                p[i]=k;                return 1;            }        }    }    return 0;}int main(){    int i,x,y;    memset(p,-1,sizeof(p)),memset(f,0,sizeof(f));    scanf("%d%d",&N,&K);    for ( i=0; i<K; i++)    {        scanf("%d%d",&x,&y);        f[x-1][y-1]=1;    }    for ( i=0; i<N; i++)    {        memset(vist,0,sizeof(vist));        dfs(i);    }    int ans=0;    for ( i=0; i<N; i++)        if (p[i]!=-1)            ans++;    printf("%d\n",ans);    return 0;}